import { Box, Typography } from "@mui/material";

export default function NoRows() {
    return (
        <Box sx={{ mt: 1, margin: "1em", textAlign: "center", color:"red"}}>
        <Typography sx={{ marginTop: "30px" }}></Typography>BRAK DANYCH</Box>

    );
}